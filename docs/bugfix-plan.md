# План исправлений от 6 сентября 2026

1. [x] Закрыть обновление задачи чужой доски через `/tasks/sync`: разделить вставку и обновление, ограничить обновление доской, возвращать ошибку при конфликте ID.
2. [x] Сохранять очередь до отправки и удалять только подтверждённые операции. При ошибке останавливать отправку, сохраняя исходный порядок. Исключить параллельные отправки одной очереди внутри вкладки.
3. [x] Дать новым задачам постоянный UUID. Сохранять создание в очередь; повторный запрос с тем же UUID не создаёт дубль. Восстановить старые локальные `tmp_`-карточки.
4. [x] Направлять все изменения задач через очередь с зафиксированным адресом доски, включая удаление после анимации и авточистку. Не заменять локальные правки загрузкой с сервера до подтверждения очереди.
5. [x] Добавить регрессионные тесты доступа, повторного создания, офлайн-очереди, восстановления после перезагрузки и смены доски. Включить клиентские тесты в `make test`.

Проверки: `make test` (Go и Node.js), `go vet ./...`, `node --check static/app.js`.
Тесты обработчиков используют отдельную SQLite-базу в памяти; прогон на MariaDB и проверка интерфейса в браузере остаются отдельными интеграционными проверками. Продакшен не изменялся.
